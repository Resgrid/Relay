<UserControl x:Class="Resgrid.Audio.Relay.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Resgrid.Audio.Relay.ViewModels"
             xmlns:conv="clr-namespace:Resgrid.Audio.Relay.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LogsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" />

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,12,0,8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Min level:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox MinWidth="130"
                          ItemsSource="{Binding Levels}"
                          SelectedItem="{Binding MinimumLevel, Mode=TwoWay}" />
                <CheckBox Content="Auto-scroll" Margin="16,0,0,0"
                          VerticalAlignment="Center"
                          IsChecked="{Binding AutoScroll, Mode=TwoWay}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Copy" Appearance="Secondary" Margin="0,0,8,0"
                           Command="{Binding CopyCommand}" />
                <ui:Button Content="Clear" Appearance="Secondary"
                           Command="{Binding ClearCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="#33FFFFFF" CornerRadius="4">
            <ListBox x:Name="LogList"
                     ItemsSource="{Binding Entries}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss.fff}"
                                       FontFamily="Consolas" Opacity="0.6" Width="92" />
                            <TextBlock Text="{Binding Level}"
                                       FontFamily="Consolas" Width="80"
                                       Foreground="{Binding Level, Converter={x:Static conv:LogLevelToBrushConverter.Instance}}" />
                            <TextBlock Text="{Binding Message}"
                                       FontFamily="Consolas" TextWrapping="Wrap"
                                       Foreground="{Binding Level, Converter={x:Static conv:LogLevelToBrushConverter.Instance}}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
