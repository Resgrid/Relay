<UserControl x:Class="Resgrid.Audio.Relay.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Resgrid.Audio.Relay.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <StackPanel VerticalAlignment="Top" MaxWidth="560" HorizontalAlignment="Left">
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" />

            <ui:Card Margin="0,16,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="{Binding ProductName}" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Margin="0,4,0,0" Text="{Binding Version, StringFormat=Version {0}}" Opacity="0.75" />
                    <TextBlock Margin="0,2,0,0" Text="{Binding Copyright}" Opacity="0.6" FontSize="12" />
                </StackPanel>
            </ui:Card>

            <ui:Card Margin="0,12,0,0" Padding="20">
                <StackPanel>
                    <CheckBox Content="Run at Windows startup (start minimized to tray)"
                              IsChecked="{Binding RunAtStartup, Mode=TwoWay}"
                              IsEnabled="{Binding CanConfigureStartup}" />
                    <TextBlock Margin="0,4,0,0"
                               Opacity="0.55"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Text="Adds an entry to HKCU\Software\Microsoft\Windows\CurrentVersion\Run. Windows only." />
                </StackPanel>
            </ui:Card>

            <ui:Card Margin="0,12,0,0" Padding="20">
                <StackPanel Orientation="Horizontal">
                    <ui:Button Content="Resgrid.com"
                               Appearance="Secondary"
                               Margin="0,0,8,0"
                               Command="{Binding OpenResgridCommand}" />
                    <ui:Button Content="Documentation"
                               Appearance="Secondary"
                               Command="{Binding OpenDocsCommand}" />
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </Grid>
</UserControl>
