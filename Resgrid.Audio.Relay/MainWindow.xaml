<Window x:Class="Resgrid.Audio.Relay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resgrid Relay Monitor"
        Height="480"
        Width="760"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Text="Input Device:" />
            <ComboBox x:Name="DeviceComboBox" Width="360" DisplayMemberPath="Name" />
            <Button x:Name="ToggleButton"
                    Width="140"
                    Margin="12,0,0,0"
                    Click="ToggleMonitoring_Click"
                    Content="Start Monitoring" />
        </StackPanel>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="1"
                   Margin="0,12,0,12"
                   FontWeight="Bold"
                   Text="Idle" />

        <TextBox x:Name="EventsTextBox"
                 Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock FontWeight="Bold" Text="Min:" />
            <TextBlock x:Name="MinTextBlock" Margin="4,0,16,0" Text="0" />
            <TextBlock FontWeight="Bold" Text="Max:" />
            <TextBlock x:Name="MaxTextBlock" Margin="4,0,16,0" Text="0" />
            <TextBlock FontWeight="Bold" Text="dB:" />
            <TextBlock x:Name="DbTextBlock" Margin="4,0,16,0" Text="0" />
            <TextBlock FontWeight="Bold" Text="FFT Bins:" />
            <TextBlock x:Name="FftTextBlock" Margin="4,0,0,0" Text="0" />
        </StackPanel>
    </Grid>
</Window>
